import Head from 'next/head';

function PageMetaTags({ title, description, url }) {
    const pageTitle = `${title}${title.includes("GST & ITR Filing") ? "" : " | GST & ITR Filing"}`;

 
}

export default PageMetaTags;