const analyticsUtil = {
    trackEvents: ({ eventName, properties }) => {
        // Placeholder for event tracking
    },

    identifyUser: ({ name, userId, credits, emailId, isBusiness }) => {
        // Placeholder for user identification
    },
};

export default analyticsUtil;
